# Tracking 0.2 发布候选收口

> 执行采用 superpowers:subagent-driven-development，逐任务实现与审查。用户已确认继续本地候选准备。

**目标：** 从已评测的0.2.0-alpha.0整理出可核验的0.2.0-rc.0包与Demo，补齐发布通道、版本、说明和证据。

**架构：** 保留ByteTrack默认，OC-SORT/DeepSORT显式可选，可选同包ReID标记人体实验能力。根CPU/main与模型WASM/WebGPU边界不变；预发布npm使用next，正式版使用latest。

**依据：** SDK reports/2026-09-21-mot-reid/README.md及门户standards/v1的SDK、Demo、性能、文档与发布契约。SDK基线1e4d777，门户93b8814，复用既有独立工作树。

## 全局约束

- 中文文档、注释、提交；现有英文镜像等价。手工编辑用apply_patch，pnpm带仓库约定两个配置参数。
- 只做本地准备；可只读查询远程。无push、PR、合并、tag、npm publish或生产部署。原用户工作区和此前被拒清理的scratch保留。
- 不改关联数学、权重、预处理、阈值或API输入输出语义；旧reports及固定评测脚本中的历史版本与hash不能全局替换。
- 包版本更新为0.2.0-rc.0，运行时类型、runtime值、Demo品牌/导出、manifest当前声明同步；模型自身0.1.0不变。
- 旧0.2.0-alpha.0的MOT结果保留历史版本；通过源码差异证明本轮仅runtime版本标识变更，不将旧构建hash冒称为RC实际测量。
- 图像模式的实验状态用简短中英标签明确，保留既有布局，不增加长操作说明。默认算法无需ORT，子入口显式导入，模型默认ModelScope、另有HF。
- 先行为失败测试，再改发布脚本：预发布publish调用必须带--tag next；正式版本latest；已有相同完整性版本不发布也不变tag；tag仍精确匹配package版本。
- 完整verify一次、真实发布包消费、当前Demo浏览器（桌面/390px、中英文、三算法、ReID实测CPU/GPU）和前后标准检查。功能未变不重复全5316帧评测。

## Task 1：候选版本、发布通道与说明

SDK工作目录C:/Users/chenm/.codex/worktrees/tracking-algorithms/web-sdk-PP-Tracking。

- [x] 读取package、src的版本引用、Demo、manifest、release脚本/测试与公共文档；列明当前声明和历史证据区别。
- [x] tests/release.test.ts增加预发布不会使用latest的调用参数断言，保留已有完整性/错误/重试门禁，运行RED。
- [x] scripts/release.mjs选择明确npm发布tag：版本有预发布标识为next，正常正式版为latest；验证已有版本路径没有publish/dist-tag写操作，GREEN。
- [x] 升级当前版本至0.2.0-rc.0；修改必要测试期望而不改历史测试固定场景。保留模型版本、上游revision及sha。可维护现有常量结构，不借机重构全项目版本系统。
- [x] 新CHANGELOG候选条目（含English equivalent）覆盖0.1→0.2多算法、ReID、模型双源/许可、真实评测结论、实验边界与迁移；修正仍待MOT的当前误述，历史报告不改。
- [x] SDK README/双语公开文档当前状态同步RC，历史已验证alpha仍写原版本；依据模板新增docs/zh-CN/releases/0.2.0-rc.0.md和英文镜像，给出npm RC未发布、未来next通道、安装/可选peer、模型/CPU分别后端、局限与撤回为新版本策略，不伪造远程状态。
- [x] Demo简短ReID人体实验标签与RC版本；不改布局风格。
- [x] pnpm完整verify，通过后再运行既有真实ReID生产UI smoke适用两后端；若专门测试固定alpha版本，必要时使期望来自package当前版本，历史reports禁止改。记录运行日志和真实包摘要到.tmp/tracking-02-rc/，截图留存。
- [x] 中文本地提交明确文件，详细实施/RED-GREEN/验证/范围报告写本计划scratch task-1-report.md。独立审查后主线程继续归档。

## 最终验证与归档（主线程）

- [x] 只读核对npm当前dist-tags/version、GitHub发布流程与remote保护证据；gh复用宿主登录且不更改状态。失败如实标unknown。
- [x] 生成固定RC包和源码/构建/模型身份、本地发布checklist及双语阶段报告，注明原评测对应alpha、RC源码差异仅标识。
- [x] 运行标准after，复核包无模型/媒体/密钥与optional-peer消费。保留旧tarball/历史锁，不把版本变化误当旧归档损坏。
- [x] 当前Demo继续可在127.0.0.1:4204访问；必要时新端口，给明确URL。不启动无关服务。
- [ ] 最终整体独立审查，归档证据与中文本地提交；门户只更新规划/回执，不提前更新生产registry。按当前授权保留本地分支，不重复问执行确认。
